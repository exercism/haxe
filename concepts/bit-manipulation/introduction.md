# Introduction

Haxe has some unary and binary operators to perform bitwise manipulation:

- `&`: bitwise AND
- `|`: bitwise OR
- `^`: bitwise XOR
- `~`: bitwise NEGATION
- `<<`: shift left
- `>>`: shift right
- `>>>`: unsigned shift right